# managelocalrepo 0.1.2

- Fixed call to full_repo_dir() from release_package() which wasn't passing the 
`type` parameter in the case when `type` was "src"

# managelocalrepo 0.1.1

- UPDATE: added '3.1' to the `r_versions` argument in `release_package()`

# managelocalrepo 0.1

- Initial release
